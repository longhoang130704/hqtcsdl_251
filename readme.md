# step 0: read script excel or image 
# step 1:
```bash
run npm i
```
# step 2: chang information 
```js
const dbConfig = {
    user: 'postgres',
    host: 'localhost',
    database: 'hqtcsdl',
    password: '12345678',
    port: 5432,
};

// Đặt đường dẫn chính xác tới file Excel của bạn
const excelFilePath = 'Radiologists Report.xlsx';
```